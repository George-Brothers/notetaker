# Plan

1. Make the template catalog persisted, validated data instead of a compile-time-only list.
2. Supply the catalog to each summary pipeline run.
3. Add a Settings editor for adding, editing, and deleting templates.
4. Test, build, tag, and publish the updater release.
