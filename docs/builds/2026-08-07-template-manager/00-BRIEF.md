# Meeting summary template manager

Mr. Brothers can manage meeting-summary templates from Settings: add a template, adjust its name, description, and section instructions, and delete custom templates. The selected template must shape the next processed or reprocessed note.

The built-in General notes template remains the non-deletable fallback so an old recording can always be processed.
